{
  "name": "mvp-agent-workflow",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "agent": "node --loader ts-node/esm src/agent.ts",
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "test:contracts": "cd contracts && forge test -v"
  },
  "dependencies": {
    "viem": "^2.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
